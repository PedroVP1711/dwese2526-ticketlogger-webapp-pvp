version: '3.8'

services:
  db:
    image: mariadb:latest
    environment:
      MARIADB_ROOT_PASSWORD: Root2025
      MARIADB_DATABASE: ticket-logger-webapp
      MARIADB_USER: PedroVp
      MARIADB_PASSWORD: Pvp2025
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data: